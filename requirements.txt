# Dependências iniciais do ReformaTax Agent.
# As versões abaixo ainda não foram fixadas (==) — isso deve ser feito assim
# que o ambiente for testado pela primeira vez (pip freeze > requirements.txt).

langgraph
langchain
langchain-core
langchain-google-genai
google-generativeai
python-dotenv
fastapi
uvicorn
pydantic
pydantic-settings
