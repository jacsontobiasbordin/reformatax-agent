# Registro de Prompts

Este documento registra os prompts utilizados ao longo do desenvolvimento do
ReformaTax Agent, para fins de rastreabilidade e documentação do processo.

| # | Data | Resumo do prompt | Resultado |
|---|------|-------------------|-----------|
| 1 | 2026-07-09 | Estruturação inicial do projeto: criação da estrutura de pastas, documentação inicial (README, escopo, prompts), `.gitignore`, `requirements.txt`, `.env.example` e versionamento com Git usando commits semânticos. Nenhuma lógica em Python implementada nesta etapa. | Estrutura de pastas e arquivos criada; repositório Git inicializado na branch `main`. |
