[pytest]
markers =
    integration: testes de integração real com um provedor de LLM (não roda por padrão; requer API key real no .env e é executado manualmente com `pytest -m integration`).
addopts = -m "not integration"
